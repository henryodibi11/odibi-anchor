# Claude Code guidance

Follow [AGENTS.md](AGENTS.md) and the canonical lifecycle in [.assistant_instructions.md](.assistant_instructions.md). The public identities are `odibi-anchor` (distribution), `odibi_anchor` (import), and `anchor` (CLI). Never use `workspace/.active_project` as server routing authority.
