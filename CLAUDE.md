# Claude Code guidance

Read every applicable
`AGENTS.md`, then follow the canonical lifecycle in [.assistant_instructions.md](.assistant_instructions.md) through the host adapter. The public identities are `odibi-anchor` (distribution), `odibi_anchor` (import), and `anchor` (CLI). Never use `workspace/.active_project` as server routing authority.
